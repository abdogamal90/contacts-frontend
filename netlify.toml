# API + Socket.io are proxied in `src/server.ts` (`netlifyCommonEngineHandler`) because the
# Angular SSR edge function runs on `/*` before these redirects would apply.

# Angular client-side routes (static assets are served first by Netlify)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
